# Server only. Never expose this value through VITE_ variables or commit a real .env file.
OPENAI_API_KEY=
# Optional; defaults to the versioned analyst configuration.
OPENAI_MODEL=gpt-5.5
# Optional local analyst server port.
ANALYST_PORT=8787
